# Tests

本目录用于保存 C 项目的最小测试代码。

当前阶段：
- 先从 test_judge.c 开始
- 不急着引入复杂测试框架
- 先使用 gcc + 手动断言 / 输出验证

测试原则：
1. 每个模块至少有正常输入和边界输入。
2. 领域阈值测试必须引用 card_id。
3. 发现 bug 后同步写入 bug_lab。
4. 测试必须保护旧功能，不允许因为重构破坏 Stage 1 单文件参考版。
