# Bridge Log

> 每日覆盖写入。用于记录当天 bridge 层的学习进度与理论反向加固推进，不替代主线代码执行记录。

- 2026-04-25：Day 17 - 03-structs
  - 当前主线文件：`exercises/03-structs/1.c`
  - 今日午间学习重点：从“散变量思维”过渡到“结构体记录思维”，围绕 `struct` 的基础组织能力做最小闭环。
  - 具体进展：
    - 理解了为什么 `struct` 更适合表示一条 pond record，而不是继续用多组散变量并排维护。
    - 进一步明确了 `PondRecord` 是自定义类型，`record` 是该类型变量。
    - 开始建立 `main` 只负责总控、输入/判断/输出逐步拆入独立函数的项目组织意识。
    - 对 `record.temp` 与 `record->temp` 的区别进行了初步厘清，知道 `.` 面向结构体本体，`->` 面向结构体指针。
    - 结合项目场景理解了为什么温度、溶氧阈值更适合提成常量统一管理，而不应散落在多个 `if` 判断中。
  - 今日 bridge 层收束：
    - 当前推进重点不是扩功能，而是把后续 `read_pond_record(PondRecord *record)` 所需的类型、成员访问、函数分层这几个概念先铺稳。
    - 为下一步在 `exercises/03-structs/1.c` 中继续推进结构体输入与记录组织做准备。
