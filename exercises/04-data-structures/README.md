# 04-data-structures

这个目录已经恢复，但当前暂不放正式 `.c` 主线文件。

原因：

- `01-basics/8.c` 里的数组、二维数组、字符串，只是数据结构前置地基
- 当前仓库里还没有稳定到可以单独抽出的：
  - stack / queue
  - ring buffer
  - hash table
  - DFS / graph traversal

后续适合回流到这里的内容包括：

- ring queue
- stack / queue
- array vs linked list
- hash table
- DFS / BFS

也就是说：

- 这里现在是“主题占位”
- 不是空缺，而是等待真正稳定的数据结构代码进来
