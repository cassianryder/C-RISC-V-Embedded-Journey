## 学习进度
- 2026-03-29：Day 1 - 01-basics:掌握关键字、标识符、数据类型（int/float/char/unsigned）、常量（数值/字符/字符串 + 转义）、变量、const、算术运算符。代码在 exercises/01-basics/1.c
- 2026-03-30：Day 2 - 01-basics:算术运算符，自增，自减（前置与后置，分别在printf中先计算后赋值，先赋值后计算），关系运算符，逻辑运算符，位运算符，补码（模运算）。代码在 exercises/01-basics/2.c 
- 2026-04-01: Day 3 -01-basics:赋值运算符，其他运算符（sizeof，三目运算符，逗号运算符），运算符的优先级和结合性，使用括号括起来的优先级最高，结合性只有三类是右结合性，分别位单目运算符号、赋值运算符号和三目运算符，表达式，语句（空语句，流程控制语句，表达式语句，复合语句，函数调用语句），流程控制语句（if语句）。代码在exercises/01-basics/3.c exercises/01-basics/4.c exercises/01-basics/5.c
- 2026-04-02: Day 4 -01-basics:if语句（if else语句，if else if语句），switch语句，if else if语句与switch语句的区别，for 语句，while语句，do while语句，跳转语句。代码在exercises/01-basics/6.c exercises/01-basics/7.c
- 2026-04-03: Day 5 -01-basics:跳转语句（break,continue,goto),数组，内存地址。代码在exercises/01-basics/7.c exercises/01-basics/8.c
- 2026-04-04: Day 6 -01-basics:int类型与进制输出，char类型常量变量补充。代码在exercises/01-basics/1.c exercis/01-basics/2.c
- 2026-04-06: Day 7 -01-basics:将char常量与变量补充部分完结，二维数组（初始化的三种方式，给出所有元素的值，给出部分元素的值，剩余的为0值，给出所有元素，可缺省最高维），makefile添加功能（make g，make asm，make mem）。代码在exercises/01-basics/2.c exercis/01-basics/8.c
- 2026-04-07: Day 8 -01-basics:字符数组(赋值方式，字符串使用 \0表示结束)。代码在exercises/01-basics/8.c
- 2026-04-08: Day 9 -01-basics:函数（函数的定义和声明），函数的基本格式（函数返回值类型，函数名，参数列表，函数体），函数的调用（库函数先导入头文件，自定义函数先定义或者先声明），声明格式（函数名+形参列表）。代码在在exercises/01-basics/1.c exercis/01-basics/9.c
- 2026-04-08: Day 10 -01-basics:函数形参与实参，值传递与址传递（在函数内部通过地址的方式修改实参），指针，数组作为函数参数时退化成指针。代码在在exercises/01-basics/1.c exercis/01-basics/10.c
- 2026-04-16: Day 11 -01-basics:函数形参与实参，函数的嵌套调用，指针，寻址，水产闭环小项目池塘水质监控系统（溶氧，温度，函数嵌调用）。代码在在exercises/01-basics/11.c 
- 2026-04-17: Day 12 -01-basics:函数形参与实参，函数的嵌套调用，水产闭环小项目池塘水质监控系统（溶氧，温度，函数嵌调用）优化两轮，c primer配套练习，getchar与putchar。代码在在exercises/01-basics/11.c
- 2026-04-18: Day 12 -01-basics:全局变量 静态变量（在静态内存空间随着程序的结束而终止）局部变量（随机，水调用结束二终止），函数的嵌套调用，c primer配套练习优化打印个人信息（for后面不加括号下一行，单人座），putchar实现（缓冲区buffer，文件io，系统调用（用户态内核态），write（）函数用法）。代码在在exercises/01-basics/11.c 
- 2026-04-20: Day 13 -01-basics:从11.c推进到12.c，完成自定义字符输出系统最小闭环：`my_putchar`、`my_flush`、`my_puts`、`my_putstr`、`my_ponds_status`。重点理解了 `getchar/putchar` 风格接口、`const char *s` 作为字符串入口、`'\0'` 作为字符串结束标记、`return c` 与 `return count` 的返回值链路、局部长度 `count` 与整行长度 `total` 的关系、`+=` 用于分段累计总长度、单字符输出与字符串输出的分层设计，以及 `printf` 和 `write` 混用时的缓冲与换行现象。水产耦合：输出单行池塘状态记录并统计记录长度。代码在 exercises/01-basics/12.c 
- 2026-04-21: Day 14 -01-basics:在 exercises/01-basics/12.c 中继续推进自定义输出系统，从固定字符串输出推进到基于温度与溶氧数值的状态判断输出。完成 `print_temp_status()` 与 `print_oxygen_status()` 两个判断函数，并在 `main` 中实现最小池塘状态记录输出：`pond A temp:Normal oxygen:Low`。重点理解了判断层与记录层的区别、字符串输出函数与单字符输出函数的分工、函数调用与字符串字面量的区别、输入格式 `%f %f` 与 `%f/%f` 的差异，以及旧测试代码与当前主线代码混存时如何收敛主线。水产耦合：根据水温和溶氧自动输出池塘状态文本。代码在 exercises/01-basics/12.c
- 2026-04-22: Day 15 -01-basics:在 exercises/01-basics/13.c 中结合 C Primer 练习做基础能力巩固，重点不再推进主项目代码，而是补齐后续推进所需的底层理解。实际运行并观察了递归函数 `up_and_down()` 中局部变量地址输出，开始把 `%p`、局部变量地址、递归调用与栈帧联系起来；同时整理了输入缓冲清理 `while(getchar() != '\n');`、`scanf` 返回值、字符串与字符常量区别、`strlen` 与 `sizeof` 的区别、`size_t` 与 `%zd`、以及 `const`/宏在配置中的使用场景等高价值问题。今日产出主要是问题沉淀与概念校准，为明日继续从 `12.c` 推进“数值 + 状态”的更完整池塘记录输出做准备。代码在 exercises/01-basics/13.c
- 2026-04-24: Day 16 -01-basics:在 exercises/01-basics/14.c 中结合 C Primer 习题继续做基础能力补强。重点接触了函数声明为什么要先于调用、参数类型不匹配时的隐式类型转换与数据丢失、递归函数 `to_binary()` 如何借助调用栈按高位到低位输出二进制、三目运算符 `?:` 作为表达式传给 `putchar()` 的方式，以及 `unsigned long` 与 `%lu` 配套使用的意义。今天主线不是继续扩项目，而是为后续项目代码重构与函数拆分补函数抽象、类型匹配和递归输出这几个地基。明日自然衔接：回到 `12.c`，把“状态判断输出”继续推进成“数值 + 状态”的更完整池塘记录输出。代码在 exercises/01-basics/14.c
- 2026-04-25: Day 17 -03-structs / 06-projects:今天的核心不是继续堆功能，而是把主线从 `01-basics` 的学习现场收口到更清晰的主题结构。完成了三条干净主线的整理：`exercises/02-pointers/1.c`、`exercises/03-structs/1.c`、`exercises/06-projects/1.c`，并同步更新 `Makefile` 入口，使 `make 21 / 31 / 61` 分别对应指针、结构体和项目种子线。重点理解了 `PondRecord` 作为类型、结构体变量、形参、实参之间的层级区别，明确了 `record.temp` 与 `record->temp` 的下一步衔接，也重新校准了“函数职责边界”和“副线项目应以能力回流而非整仓并入”的工程判断。今天同时完成了主仓 README 的公开化压缩、主线切换到 `exercises/03-structs/1.c`、以及副线搭配主线路线图沉淀，为后续推进 `read_pond_record(PondRecord *record)` 和结构体输入封装做好了结构准备。代码在 exercises/02-pointers/1.c exercises/03-structs/1.c exercises/06-projects/1.c
- 2026-04-27: Day 18 -03-structs:今天围绕 `exercises/03-structs/1.c` 做结构体主线闭环，把输入层从 `main` 中拆出，完成 `read_pond_record(PondRecord *record)` 的最小封装。重点分析了 `PondRecord` 作为类型、`PondRecord record;` 作为结构体对象、`&record` 作为对象地址这三层的区别，也把 `record.temp` 与 `record->temp` 的边界落到了 `scanf("%f %f %c", &record->temp, &record->oxygen, &record->pond_id)` 这一类实际代码中。同时重新校准了“值传递适合读和打印、指针传递适合写和修改原对象”的接口意识，以及结构体底层可理解为连续内存布局但成员间可能存在 padding 的直觉。把结构体、地址和函数职责边界接通，为后续把这套输入封装迁回 `exercises/06-projects/1.c` 做好了准备。代码在 exercises/03-structs/1.c
- 2026-04-29: Day 19 -03-structs / 06-projects / topic split:今天继续围绕 `exercises/03-structs/1.c` 收结构体样板，不再把它只当“能跑的练习”，而是开始按“样板文件 + 学习空间”双重定位来理解。重点吃透了 `PondRecord *record` 作为“接收结构体对象地址的指针变量”、`record->temp == (*record).temp`、`&record->temp == &((*record).temp)` 这条链，以及 `scanf` 返回的是“成功匹配并赋值的项目数”，所以输入 `q` 更接近返回 `0`，`Ctrl-D` 更接近 `EOF`。在此基础上，把已经验证稳定的输入封装迁回 `exercises/06-projects/1.c`，让项目种子线吸收 `read_pond_record(PondRecord *record)` 这套模式；同时将 `01-basics/15.c` 中混合的指针基础、数组组织、指针步长、结构体地址桥接和 `sizeof` 补强内容拆到 `02-pointers/2.c`、`03-structs/2.c`、`04-data-structures/1.c`、`05-embedded/2.c`、`07-basics-add/2.c`，保留 `15.c` 为学习现场。今天的主线收获不是功能暴涨，而是把结构体样板、项目种子线、输入返回值语义和主题拆分真正串成了更清晰的整体架构。代码在 exercises/03-structs/1.c exercises/06-projects/1.c exercises/01-basics/15.c 及新拆出的 02/03/04/05/07 主题文件
- 2026-04-30: Day 20 -06-projects:今天围绕 `exercises/06-projects/1.c` 推进“可保存记录”的前置层。完成了阈值常量化、状态判断从直接打印到 `const char *` 返回状态文本的设计转向，并系统拆解了 `time_t -> struct tm -> strftime` 的时间戳链路。重点误区是：`temp_status(...) == "low"` 在 C 中比较的是地址而不是字符串内容；`time_t` 不是打印工具，而是机器可计算的时间值；`record->sampled_at` 作为数组成员传参时退化为首元素地址，适合作为 `strftime` 的写入缓冲区。当前时间戳代码仍需明日收口，主线目标是先让带时间戳的终端输出重新编译通过，再进入 CSV 保存。代码在 exercises/06-projects/1.c
- 2026-05-02: Day 21 -06-projects:今天围绕 `exercises/06-projects/1.c` 从带时间戳终端输出推进到 CSV 保存雏形。真实问题集中在四条线上：`time(NULL)` 与数组访问符号混淆、`read_pond_record()` 中为什么继续传 `record` 而不是 `&record`、普通函数“成功 1/失败 0”和 `main return 0` 的层级差异、以及 `FILE * / fopen("a") / fprintf / fclose` 这套文件 I/O 现在该学到什么深度。代码层面发现 `save_pond_record_csv()` 的返回值合同还需统一，保存动作也应进入有效记录循环内部。明日主线：先修返回值和保存位置，再完整跑通“输入 -> 打印 -> CSV 追加保存”。代码在 exercises/06-projects/1.c
- 2026-05-04: Day 22 -06-projects:今天只有约 30 分钟，因此采用小闭环推进，不新增问题整理。围绕 `exercises/06-projects/1.c` 完成 CSV 保存流程收口：将 `save_pond_record_csv()` 的返回值合同统一为“保存成功返回 1，保存失败返回 0”，并把保存动作接入 `while (read_pond_record(&record))` 的有效记录循环内部。今天重点理解了“函数放在 `if` 条件里也会真实执行”，因此删除了重复调用 `save_pond_record_csv(record)` 导致一条记录保存两次的问题。已用临时目录中的模拟输入验证链路：输入一条记录 -> 填时间戳 -> 终端打印 -> CSV 追加一行 -> 输入 `q` 退出。明日主线：整理今天与明天合并的问题记录，并继续把 CLI 存储层做得更干净，包括文件名、错误提示和运行产物边界。代码在 exercises/06-projects/1.c
- 2026-05-05: Day 23 -06-projects / local_review:今天约 90 分钟，实际时间流为：SRS 抽卡与理解校准约 20-25 分钟，指针/`const char *`/函数调用表达式校准约 15-20 分钟，SRS 本地算法与规则优化约 25-30 分钟，`06-projects/1.c` 宏常量与 CSV 文件名闭环约 15-20 分钟，编译运行验证与收口约 5-8 分钟。今天把每日流程升级为“可用时间段 -> 动态 SRS -> 理解校准 -> 主线规划 -> 主逻辑挖空带练 -> 验证收口”，并在本地 SRS 中加入 `--block` 分段时间输入、智能抽卡、严格标签和校准门槛。概念上重点校准了 `if (save_pond_record_csv(record) == 0)` 的调用顺序、`record->temp / (*record).temp / &record->temp` 的层级、`const char *` 与字符串字面量、指针大小与 `PondRecord **`。代码上完成 `CSV_FILE_NAME` 宏常量化，并修正 `fopen("CSV_FILE_NAME", "a")` 这种把宏名写成字符串字面量的错误，验证临时运行目录中正确生成 `pond_records.csv`。明日主线：先处理运行产物边界和 `.gitignore`，再继续把 CSV 存储层错误路径与主线注释收干净。代码在 exercises/06-projects/1.c，SRS 工具在 skills/local_review/
- 2026-05-06: Day 24 -06-projects:今天以短闭环收口，确认 `exercises/06-projects/1.c` 已稳定跑通“结构体记录 -> 输入 -> 时间戳 -> 状态判断 -> 终端打印 -> CSV 追加保存”，并确认运行产物 `pond_records.csv` 已由 `.gitignore` 忽略，仓库状态保持干净。重点校准了 CSV 表头设计：`fopen(CSV_FILE_NAME, "a")` 中 `"a"` 是传给 `fopen` 的追加模式字符串，文件为空时应先写 header，再写当前记录，而不是跳过写入。明日主线：在 `save_pond_record_csv()` 中补 `fseek/ftell` 判断空文件并只写一次表头，同时继续保持 SRS 限时、主线优先、最后收口的节奏。
- 2026-05-07: Day 25 -06-projects / context engineering:今天围绕 `exercises/06-projects/1.c` 收 CSV 保存层，完成空文件写 header 的最小逻辑，并用临时目录验证新 CSV 第一行是表头、后续是记录。重点校准了 `FILE *fp` 是文件操作入口、`fseek(fp, 0, SEEK_END)` 移动文件位置、`ftell(fp)` 在末尾时可作为文件大小，以及 CSV 生成位置取决于运行目录。当天还经历误删仓库恢复：从 GitHub 重新 clone 主仓，重建未提交代码，并将脱敏版 `daily/`、`prompts/` 纳入 Git 白名单。SRS 侧升级为标签驱动：新增标签分组、相关标签图和主线权重，支持 `daily/daily_review.py map` 与 `--related-tag` 精准抽卡。当前项目处在 CLI 版水产记录系统的存储层阶段，下一步是收干净文件 I/O 代码风格，再进入多文件模块化和最小测试。
- 2026-05-08: Day 26 -06-projects:今天 1 小时短闭环，先用 SRS 聚焦 `FILE * / fseek / ftell / fprintf`，再回到 `save_pond_record_csv()` 收代码风格和英文注释。重点校准了 `fp` 是文件流入口，不是文件名或内容；`fseek(fp, 0, SEEK_END)` 中 `0` 是相对文件末尾的偏移量；`ftell(fp)` 在末尾时可用于判断文件大小；`fprintf` 写 CSV 字段属于存储层，`print_pond_record()` 属于终端输出层。今天还开始建立个人 C 风格：朴素 C 工程风格、失败路径提前返回、注释解释 why，并用 nvim 的缩进/选择/修改快捷键训练风格一致性。项目仍处在 CLI 池塘记录系统的存储层，下一步是决定先做最小测试还是开始多文件模块化拆分。
- 2026-05-09: Day 27 -06-projects / system dynamics:今天只有约 20 分钟，因此采用系统动力学微闭环。先把“存量-流量-关系-反馈-观察-记忆”方法沉淀成项目建模思路，再在 `exercises/06-projects/1.c` 中加入最小反馈层 `needs_aeration()`：由 `record.oxygen` 和 `OXYGEN_LOW_LIMIT` 推导是否需要增氧提醒。随后新建 `exercises/06-projects/2.c`，只保留水产养殖系统的存量、流量、参数、反馈和 `main` 时间推进骨架，并在 `Makefile` 增加 `make 62` 入口验证可编译运行。今天的重点不是功能扩展，而是把你的系统动力学想法落实为 C 项目设计流程：先建模，再映射数据结构，再写 `main` 骨架，再逐步补函数。下一步自然衔接是在 `2.c` 中选择一个最小反馈函数实现，或回到 `1.c` 收 `needs_aeration()` 的代码风格与验证。
- 2026-05-11: Day 28 -06-projects / system feedback:今天回到 `exercises/06-projects/1.c` 完成低氧反馈闭环：`needs_aeration()` 判断、`print_aeration_action()` 显示动作、`print_oxygen_alert()` 显示告警、`save_oxygen_alert_csv()` 保存低氧事件，并将告警文件名收敛为 `OXYGEN_ALERT_FILE_NAME`。重点校准了函数作用域、判断层复用、可解释输出、`printf` 参数边界、无告警正常跳过的 `return 1`、以及 `if (!save...)` 作为异常处理。另在 `/Users/a15951407904/projects/C-Journey-Framework` 建立双语教学地图和可运行参考项目，用于定位当前 Stage 1 与后续多文件、测试、传感器模拟、控制层的衔接。下一步：收干净运行产物和旧注释，再准备 Stage 2 多文件拆分。
- 2026-05-12: Day 29 -06-projects / alert memory:今天用 2 小时闭环先做 SRS，再回到 `exercises/06-projects/1.c` 的告警记忆层，完成 `oxygen_alert.csv` 空文件写 header，并把告警记录升级为 `sampled_at,pond_id,event,oxygen,oxygen_low_limit` 格式。重点校准了 `OXYGEN_ALERT_FILE_NAME` 是文件名、`fp` 才是文件入口，`ftell(fp)` 返回当前位置相对文件开头的偏移量，以及 `LOW_OXYGEN` 与 `"LOW_OXYGEN"` 的区别。当前 Stage 1 的“反馈事件 -> 结构化记忆”更稳定；下一步优先处理运行产物 `.gitignore`、旧注释收口，再考虑控制状态或多文件拆分。
- 2026-05-13: Day 30 -project goal / planning framework:今天不直接写 C 代码，而是完成项目目标升级和主线规划方法沉淀。长期目标明确为“日本沼虾多塘口养殖辅助决策系统”：第一版只做 C 命令行、多塘口结构、CSV、阈值判断和控制日志，后续再接 Milk-V Duo S、传感器、继电器、天气 API、边缘端、可视化和机器学习。新增 `notes/project_goal.md` 作为长期项目记忆，并更新 `notes/current.md`。同时把每日主线规划固化为 7 问流程：现实问题、系统动力学变量、C 映射、代码证据、运行证据、教材补洞、GitHub 沉淀；该流程已写入 `skills/methodology/skill_system_dynamics_to_c.md`、`prompts/core_codex_system.md` 和 `prompts/daily_input_template.md`。下一步回到 `exercises/06-projects/1.c`，以多塘口 MVP 为主线，先从 `Pond` / `PondRecord` / 结构体数组 / 阈值判断 / CSV 记录的小闭环开始。
- 2026-05-14: Day 31 -06-projects / control boundary:今天 1 小时闭环，先用 SRS 校准 `FILE *fp`、`fseek(fp, 0, SEEK_END)`、`ftell(fp)` 与 CSV 空文件判断，再回到 `exercises/06-projects/1.c` 收低氧反馈到设备控制的语义边界。完成 `aerator_should_be_on()`，让它复用 `needs_aeration(record)`，并把 `print_aeration_action()` 从直接询问水质判断改为询问控制层判断。重点理解了 `needs_aeration()` 回答“是否低氧”，`aerator_should_be_on()` 回答“增氧机是否应该开”，低氧只是未来手动/自动/定时/设备故障等控制条件中的一种。运行验证通过：低氧样例输出 `aeration ON` 并写入告警 CSV，正常氧样例输出 `aeration OFF`；同时将 `oxygen_alert.csv` 加入 `.gitignore`，明确运行数据不提交。下一步进入多塘口 MVP 前，可继续把控制日志 `control_log.csv` 与水质告警日志边界拆清楚。
