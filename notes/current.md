# Current Snapshot

> 每天覆写一次，不做历史累积；供 GPT Project 读取当前主线进度、问题与自然衔接。

- 当前主线文件：`exercises/01-basics/12.c`

- 当前主攻知识点：`状态判断输出`、`字符串/单字符分层`、`自定义输出系统`

- 最近已完成：
  - 在 `12.c` 中完成 `my_putchar`、`my_flush`、`my_puts`、`my_putstr` 的最小闭环。
  - 从固定字符串输出推进到 `print_temp_status()`、`print_oxygen_status()` 的数值判断输出。
  - 在 `main` 中实现最小池塘状态记录输出：`pond A temp:Normal oxygen:Low`。

- 当前未解决问题：
  - `12.c` 中仍混有旧测试代码与长度统计测试，主线还需要收口。
  - `printf` 与 `write` 仍在混用，输出体系尚未完全统一。
  - 当前只输出“状态文本”，还没有输出“数值 + 状态”的更完整记录格式。
  - `my_ponds_status()` 与新的判断层函数还没有统一成一个更清晰的接口。

- 下一步自然衔接：
  - 清理 `12.c` 中的旧测试代码，只保留当前唯一主线。
  - 把输出推进到“数值 + 状态”，例如：`temp:27.5(normal)`、`oxygen:4.2(low)`。
  - 视进度把温度阈值、溶氧阈值抽成常量，为后续结构体与多字段记录做准备。
