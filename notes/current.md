# Current Snapshot

> 每天覆写一次，不做历史累积；供 GPT Project / Codex / 理论反向加固系统读取当前主线、问题维度与自然衔接。

- 当前长期目标：用 C 实现一个日本沼虾多塘口养殖辅助决策系统。第一版聚焦命令行、多塘口结构、CSV、阈值判断和控制日志；后续再接 Milk-V Duo S、传感器、继电器、边缘端和机器学习。

- 当前项目位置：`Stage 1 单文件 CLI 闭环` 基本收口，准备进入 `Stage 2 多文件模块化`。

- 当前主线文件：`exercises/06-projects/1.c`

- 学习痕迹副本：`exercises/01-basics/16.c`，只用于保留旧注释、探索过程和阶段性理解，不作为项目主线推进。

- 当前主攻知识点：`函数合同`、`FILE * 文件流状态`、`fprintf 字段拆分`、`CSV 记忆层`、`水质判断层 / 控制决策层 / 存储层分离`、`main 流程收敛`、`多文件模块化前置设计`

- 最近已完成：
  - `PondRecord -> 输入 -> 时间戳 -> 状态判断 -> 终端输出 -> CSV 追加保存`。
  - `pond_records.csv`：保存水质记录。
  - `oxygen_alert.csv`：只保存低氧水质异常事件。
  - `control_log.csv`：保存设备控制建议，低氧为 `AERATOR,ON,LOW_OXYGEN`，正常氧为 `AERATOR,OFF,NORMAL_OXYGEN`。
  - `aerator_should_be_on()`：把水质判断转接为控制决策。
  - `aerator_action()` / `aerator_reason()`：生成控制日志字段。
  - `main()` 已清理旧注释，主线文件更接近单文件参考版。

- 理论反向加固映射：
  - C 语言：函数形参与实参、返回值合同、结构体值传递、字符串字面量、函数命名、文件 I/O。
  - 计算机系统：文件流状态、缓冲区、当前位置、外部文件内容、运行产物边界。
  - 软件工程：职责分层、主函数变薄、学习副本和工程主线分离、可提交内容与运行数据分离。
  - 系统动力学：存量是记录字段，关系是判断函数，反馈是低氧到增氧决策，记忆是 CSV，观察是终端输出。

- 当前未解决问题：
  - `06-projects/1.c` 仍是单文件版本，还没有拆成 `.h / .c` 模块。
  - `Pond` 多塘口对象尚未引入，目前仍是单条 `PondRecord` 输入闭环。
  - 真实传感器、继电器、天气 API、前端、AI 暂不进入当前阶段。

- 下一步自然衔接：
  - 先做 Stage 2 模块边界设计，不急着写大量代码。
  - 推荐拆分顺序：`judge` -> `control` -> `csv_store` -> `input_cli` -> `main`。
  - 下一次启发带练最小任务：列出 `06-projects/1.c` 中每个函数应该归属哪个模块。
  - 验收标准：能解释每个函数属于输入层、判断层、控制层、输出层、存储层还是流程层。
  - 继续遵守最小任务协议：每次只推进一个任务，先给函数签名/伪代码/验收标准，核心代码由用户亲手写，Codex 审查和验证。
