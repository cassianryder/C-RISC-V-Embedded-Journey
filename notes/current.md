# Current Snapshot

> 每天覆写一次，不做历史累积；供 GPT Project 读取当前主线进度、问题与自然衔接。

- 当前主线文件：`exercises/01-basics/12.c`

- 当前主攻知识点：`数值 + 状态记录输出`、`判断层到记录层过渡`、`输入缓冲/字符串长度/返回值地基补强`

- 最近已完成：
  - 在 `12.c` 中完成基于温度与溶氧数值的最小状态判断输出：`pond A temp:Normal oxygen:Low`。
  - 清理并收窄了 `12.c` 主线，旧测试代码已基本退出主线输出。
  - 在 `13.c` 中补齐了高价值地基问题：递归中的局部变量地址、输入缓冲清理、字符串与字符区别、`strlen` 与 `sizeof` 的区别。

- 当前未解决问题：
  - `12.c` 目前只输出“状态”，还没有输出“数值 + 状态”的完整记录形式。
  - `my_ponds_status()` 与新的判断函数仍是两套思路，接口还没有收拢成一个更清晰的主线版本。
  - `printf` 与 `write` 的混用问题还没有完全收口。
  - 温度阈值、溶氧阈值仍未提成常量。

- 下一步自然衔接：
  - 明天直接把 `12.c` 推进到：`pond A temp:27.5(normal) oxygen:4.2(low)`。
  - 如果进度顺利，进一步把阈值抽成常量，并收拢一个更清晰的记录输出函数。
  - 之后为结构体组织一条完整池塘记录做准备。
