# 005 - CSV Header Once v0

## 0. Metadata

packet_id: 005-csv-header-once-v0
stage: Stage 2-1
task_name: CSV Header Once v0
task_type: test_addition
execution_mode: guided_practice
human_must_execute: true
codex_can_modify_files: false
codex_can_only_review: true

reference_files:
  - AGENTS.md
  - learning/c_embedded_skill_graph.yml
  - learning/aquaculture_cycle_map.yml
  - learning/daily_planning_policy.md
  - domain_snapshot/cards/
  - domain_snapshot/source_index.md
  - domain_snapshot/gap_list.md
  - codex/daily_packets/001-judge-module-v0.md
  - codex/daily_packets/002-test-judge-build-v0.md
  - codex/daily_packets/003-control-module-v0.md
  - codex/daily_packets/004-csv-store-module-v0.md
  - docs/daily/2026-05-23-judge-module-v0.md
  - docs/daily/2026-05-24-test-judge-build-v0.md
  - docs/daily/2026-05-25-control-module-v0.md
  - docs/daily/2026-05-26-csv-store-module-v0.md
  - bug_lab/2026-05-23-judge-parameter-contract.md
  - bug_lab/2026-05-24-judge-build-link-error.md
  - bug_lab/2026-05-25-control-interface-contract.md
  - bug_lab/2026-05-26-csv-store-file-mode.md
  - notes/current.md
  - Makefile
  - include/record.h
  - include/judge.h
  - include/control.h
  - include/csv_store.h
  - src/judge.c
  - src/control.c
  - src/csv_store.c
  - tests/test_judge.c
  - tests/test_control.c
  - tests/test_csv_store.c
  - exercises/06-projects/1.c

target_files_user_may_edit:
  - src/csv_store.c
  - tests/test_csv_store.c
  - docs/daily/2026-05-27-csv-header-once-v0.md
  - bug_lab/2026-05-27-csv-header-duplication.md

readonly_files_for_codex:
  - src/csv_store.c
  - tests/test_csv_store.c
  - docs/daily/2026-05-27-csv-header-once-v0.md
  - bug_lab/2026-05-27-csv-header-duplication.md
  - include/csv_store.h
  - include/record.h
  - include/judge.h
  - include/control.h
  - src/judge.c
  - src/control.c
  - tests/test_judge.c
  - tests/test_control.c
  - exercises/06-projects/1.c
  - Makefile

forbidden_files:
  - domain_snapshot/
  - domain_snapshot/cards/
  - domain_snapshot/source_index.md
  - domain_snapshot/gap_list.md
  - src/input_cli.c
  - include/input_cli.h
  - src/output.c
  - include/output.h
  - main.c
  - hardware/
  - drivers/
  - frontend/
  - database/
  - .gitignore
  - build/
  - tmux-*.log

domain_cards:
  - WQ-DO-GROWOUT-001

related_gaps:
  - GAP-001: Growout temperature status thresholds

bug_lab_required: true
bug_lab_mode: controlled_injection
bug_lab_design_by: codex
bug_lab_injection_by: human
bug_lab_repair_by: human
bug_lab_review_by: codex

srs_required: true
srs_source:
  - docs/daily/2026-05-26-csv-store-module-v0.md
  - bug_lab/2026-05-26-csv-store-file-mode.md
  - current_main_task
  - current_bug_lab

git_commit_required: true

## 1. 今日主线

今日只解决一个核心目标：

```text
用户本人手动增强 csv_store 模块，使 CSV 表头只在新文件中写入一次。
```

今日不是新增完整日志系统，不是接 main，不是生成时间戳，不是做 CSV 读取解析，不是数据库。

今日完整闭环是：

```text
上一个闭环 SRS 抽卡
→ 用户手动阅读 csv_store / test_csv_store / Makefile
→ Codex 解释“表头只写一次”的最小工程边界
→ 用户手动修改 src/csv_store.c
→ 用户手动修改 tests/test_csv_store.c
→ 用户手动运行 make test_csv_store
→ 用户手动运行 make test
→ 用户手动做 Clean Baseline
→ Codex 设计 Bug Lab 受控注入
→ 用户手动注入 bug
→ 用户手动触发失败
→ 用户手动修复
→ 用户手动写 bug_lab
→ 用户手动写 docs/daily
→ Codex 审查
→ 用户手动 Git 提交
```

今日系统链路保持：

```text
record -> csv_store
```

不接入：

```text
judge
control
main
hardware
```

今日 csv_store v1 的目标：

```text
如果目标 CSV 文件是新文件或空文件，则先写表头。
如果目标 CSV 文件已有内容，则只追加记录，不重复写表头。
```

建议表头：

```text
pond_id,temp,oxygen
```

如果 `include/record.h` 中字段名不同，必须以真实字段为准，不允许编造字段。

今日重点训练能力：

```text
1. FILE* 文件位置判断
2. fopen 读写模式
3. fseek / ftell 或等价文件空判断
4. CSV 表头策略
5. 测试“第一次写入”和“第二次写入”的差异
6. 文件 I/O 行为测试
7. Bug Lab 表头重复写入注入
8. SRS 抽卡
9. 王道 408 文件系统映射
10. Git 最小提交
```

Codex 在今日主线中的角色：

```text
带练、解释、审查、提问、设计 Bug Lab，不代工。
```

## 2. 今日不做

今日明确不做：

```text
1. 不做时间戳。
2. 不做 CSV 读取解析模块。
3. 不做数据库。
4. 不接 main。
5. 不拆 input_cli 模块。
6. 不拆 output 模块。
7. 不重构 judge 模块。
8. 不重构 control 模块。
9. 不重构 record 结构体。
10. 不接硬件。
11. 不接 Milk-V Duo S。
12. 不写 GPIO。
13. 不写 relay 控制。
14. 不写真实执行器驱动。
15. 不新增传感器逻辑。
16. 不新增前端。
17. 不做 AI 预测。
18. 不做新的领域资料检索。
19. 不做证据审核。
20. 不修改 domain_snapshot。
21. 不修改 domain_snapshot/cards/。
22. 不修改 domain_snapshot/source_index.md。
23. 不修改 domain_snapshot/gap_list.md。
24. 不新增日本沼虾温度阈值。
25. 不把 temp_status() 的 legacy 行为解释成领域规则。
26. 不关闭 GAP-001。
27. 不破坏 exercises/06-projects/1.c 单文件参考版。
28. 不修改 tests/test_judge.c。
29. 不修改 tests/test_control.c。
30. 不修改 include/record.h。
31. 不修改 include/judge.h。
32. 不修改 include/control.h。
33. 不修改 src/judge.c。
34. 不修改 src/control.c。
35. 不整理无关缩进。
36. 不格式化无关代码。
37. 不补充无关注释。
38. 不做顺手优化。
39. 不移动已有文件。
40. 不让 Codex 自动修改文件。
41. 不让 Codex 自动生成 docs/daily 成品。
42. 不让 Codex 自动生成 bug_lab 成品。
```

今日所有动作必须服务于：

```text
CSV 表头只写一次 + 闭环末端 Bug Lab 训练
```

## 3. 证据与 gap 约束 + 上一个闭环 SRS 抽卡

### 3.1 领域证据约束

Codex 和用户必须先确认：

```text
1. 当前任务是否涉及领域判断：不涉及新的领域判断。
2. 是否涉及养殖阈值：不涉及。
3. 是否涉及病害判断：不涉及。
4. 是否涉及投喂比例：不涉及。
5. 是否涉及增氧建议：不涉及新的增氧建议。
6. 是否涉及 open gap：涉及 GAP-001，但只作为禁止约束。
```

当前允许继续存在：

```yaml
card_id: WQ-DO-GROWOUT-001
source_id: SRC-001
usage: oxygen_status() / needs_aeration()
status: allowed
```

csv_store 模块允许做：

```text
保存 PondRecord 原始字段。
写入 CSV 表头。
```

csv_store 模块禁止做：

```text
1. 不调用 temp_status()。
2. 不调用 oxygen_status()。
3. 不调用 needs_aeration()。
4. 不调用 control_should_aerate()。
5. 不直接写 oxygen < 5.0。
6. 不直接写任何领域阈值。
7. 不生成控制建议。
8. 不生成领域解释。
```

当前必须保持：

```yaml
function: temp_status()
status: legacy_behavior
verification: needs_human_verification
gap: GAP-001 open
```

没有领域卡支撑的内容只能标记为：

```text
needs_human_verification
```

或：

```text
hypothesis
```

不得进入正式代码逻辑。

### 3.2 gap_list 约束

当前相关 gap：

```text
GAP-001: Growout temperature status thresholds
```

约束如下：

```text
1. GAP-001 仍为 open。
2. temp_status() 不得作为 evidence-based domain rule。
3. csv_store 不得新增任何温度状态解释。
4. csv_store 不得新增任何 DO 阈值解释。
5. csv_store 不得新增控制规则。
6. 不得新增温度阈值。
7. 不得关闭 GAP-001。
8. 不得在 docs/daily 中写“温度规则已证据化”。
9. 今日不是 domain_rule_update 类型任务。
```

### 3.3 上一个闭环 SRS 抽卡要求

每日任务开始前，必须从上一个闭环抽取 SRS。

SRS 来源：

```text
1. docs/daily/2026-05-26-csv-store-module-v0.md
2. bug_lab/2026-05-26-csv-store-file-mode.md
3. fopen "a" vs "w"
4. filename vs FILE *fp
5. CSV 作为最小持久化层
```

Codex 第一轮必须抽取 3-5 张 SRS 候选卡，但只提问，不代替用户答。

本次至少抽问：

```text
Q1: fopen(filename, "a") 和 fopen(filename, "w") 的区别是什么？
A1: 用户回答后 Codex 校准。

Q2: 为什么养殖记录系统需要 append，而不是 overwrite？
A2: 用户回答后 Codex 校准。

Q3: filename 和 FILE *fp 的区别是什么？
A3: 用户回答后 Codex 校准。

Q4: 为什么测试前要 remove(filename)？
A4: 用户回答后 Codex 校准。

Q5: csv_store 为什么不应该调用 judge 或 control？
A5: 用户回答后 Codex 校准。
```

SRS 卡片分类：

```text
C 文件操作
FILE*
CSV 持久化
Makefile / 构建类
模块边界类
Bug 归因类
领域证据类
408 映射类
```

后期这些 SRS 可以整理为类似 Anki 的抽卡系统：

```text
daily_srs/cards/
daily_srs/review_log/
daily_srs/tag_index.yml
```

但今日不强制生成 Anki 文件，只要求：

```text
1. 今日开始前抽问。
2. 今日结束后新增 SRS 候选。
3. docs/daily 中记录。
```

## 4. 用户手动执行步骤

Codex 不允许替用户执行闭环。

用户必须亲自完成：

```text
1. 打开 nvim。
2. 阅读 Makefile。
3. 阅读 include/record.h。
4. 阅读 include/csv_store.h。
5. 阅读 src/csv_store.c。
6. 阅读 tests/test_csv_store.c。
7. 手动修改 src/csv_store.c。
8. 手动修改 tests/test_csv_store.c。
9. 手动运行 make test_csv_store。
10. 手动运行 make test。
11. 手动运行 git diff --check。
12. 手动运行 Clean Baseline 检查。
13. 手动注入 Bug Lab bug。
14. 手动触发 bug。
15. 手动修复 bug。
16. 手动写 bug_lab。
17. 手动写 docs/daily。
18. 手动 git add / commit。
```

今日主线建议手动步骤：

```bash
# 1. 查看当前状态
git status

# 2. 阅读现有 csv_store / record / Makefile
sed -n '1,160p' include/record.h
sed -n '1,160p' include/csv_store.h
sed -n '1,220p' src/csv_store.c
sed -n '1,260p' tests/test_csv_store.c
sed -n '1,260p' Makefile

# 3. 手动修改 csv_store 实现
nvim src/csv_store.c

# 4. 手动修改 csv_store 测试
nvim tests/test_csv_store.c

# 5. 运行测试
make test_csv_store
make test

# 6. 检查 diff
git diff
git diff --check

# 7. Clean Baseline 检查
git diff -- tests/test_judge.c tests/test_control.c
git diff -- include/record.h include/judge.h include/control.h
git diff -- src/judge.c src/control.c
git diff -- domain_snapshot
git diff -- exercises/06-projects/1.c

# 8. 将输出贴给 Codex 审查
```

建议实现方向：

```text
1. 打开文件时需要能判断是否为空。
2. 如果文件为空，则先写表头。
3. 然后写入 PondRecord 数据行。
4. 后续再次 append 时，不重复写表头。
```

可选策略：

```text
A. 使用 fopen(filename, "a+")，再 fseek/ftell 判断文件大小。
B. 使用先尝试读取，再决定是否写表头。
```

建议优先使用：

```text
fopen(filename, "a+")
fseek(fp, 0, SEEK_END)
long size = ftell(fp)
if (size == 0) 写表头
```

注意：

```text
不要引入时间戳。
不要引入复杂 CSV 解析。
不要修改 PondRecord 字段。
```

建议表头：

```text
pond_id,temp,oxygen
```

字段必须以当前 `include/record.h` 为准。

建议测试方向：

```text
1. remove(filename) 清理旧测试文件。
2. 调用 csv_store_append_record 写入第一条记录。
3. 调用 csv_store_append_record 写入第二条记录。
4. 打开文件读取行数。
5. 期望总行数为 3：1 行 header + 2 行 data。
6. 检查 header 只出现 1 次。
7. 不测试时间戳。
8. 不调用 judge/control。
```

Codex 只能在用户每一步之后做：

```text
解释
检查
纠偏
提问
建议下一步用户亲自做什么
```

## 5. Codex 带练规则

Codex 只能作为带练。

允许：

```text
1. 读取任务包。
2. 读取文件。
3. 解释当前状态。
4. 给出用户手写 patch 的建议。
5. 解释每一行 FILE* / fseek / ftell / fprintf 的含义。
6. 提问上一个闭环 SRS。
7. 设计受控 Bug Lab。
8. 审查用户贴出的 diff。
9. 判断错误类型。
10. 审查用户写的 docs/daily 草稿。
11. 审查用户写的 bug_lab 草稿。
12. 给 git add 白名单。
13. 给 commit message 建议。
```

禁止：

```text
1. 自动修改文件。
2. 自动生成最终 docs/daily。
3. 自动生成最终 bug_lab。
4. 自动修复 bug。
5. 自动 git add。
6. 自动 git commit。
7. 自动修改 domain_snapshot。
8. 代替用户完成闭环。
```

Codex 每次输出都必须包含：

```text
1. 下一步用户亲自做什么。
2. 为什么做。
3. 做完后把什么结果贴回来。
```

## 6. 禁止代工规则

本任务包默认：

```yaml
codex_can_modify_files: false
```

Codex 不允许执行：

```text
1. 直接改 Makefile。
2. 直接改 C 文件。
3. 直接改 tests。
4. 直接写 docs/daily 成品。
5. 直接写 bug_lab 成品。
6. 直接运行破坏性命令。
7. 直接 git add / commit。
8. 直接关闭 gap。
9. 直接生成领域事实。
10. 直接注入 bug。
11. 直接修复 bug。
12. 直接修改 src/csv_store.c。
13. 直接修改 tests/test_csv_store.c。
```

如果用户明确要求 Codex 代工，Codex 必须先提醒：

```text
这会削弱训练闭环。当前系统默认要求用户亲自执行。
```

只有用户再次确认：

```text
允许 Codex 代工
```

并且任务不涉及领域规则、open gap、Bug Lab 训练，才可进入 `engineering_execution`。

本任务包不允许切换为 engineering_execution。

## 7. 主线验收标准

今日主线完成必须同时满足：

```text
1. 用户本人完成 src/csv_store.c 修改。
2. 用户本人完成 tests/test_csv_store.c 修改。
3. 用户本人运行 make test_csv_store。
4. 用户本人运行 make test。
5. 测试通过。
6. CSV 新文件首次写入时包含 1 行表头。
7. CSV 连续写入两条记录后总行数为 3。
8. 表头只出现 1 次。
9. git diff 只包含目标文件。
10. tests/test_judge.c 无 diff。
11. tests/test_control.c 无 diff。
12. include/record.h 无 diff。
13. include/judge.h 无 diff。
14. include/control.h 无 diff。
15. src/judge.c 无 diff。
16. src/control.c 无 diff。
17. domain_snapshot 无 diff。
18. exercises/06-projects/1.c 无 diff。
19. csv_store 不调用 judge。
20. csv_store 不调用 control。
21. csv_store 不写领域阈值。
22. csv_store 不处理 temp_status。
23. csv_store 不做时间戳。
24. csv_store 不接 main。
25. 没有无关格式化。
26. 没有 Codex 代工。
27. docs/daily 有真实操作记录。
28. SRS 有新增候选。
29. Bug Lab 完成受控注入和修复。
30. 408 映射完成，并尽量细到王道小节。
```

主线验收命令：

```bash
make test_csv_store
make test
git diff
git diff --check
git diff -- tests/test_judge.c tests/test_control.c
git diff -- include/record.h include/judge.h include/control.h
git diff -- src/judge.c src/control.c
git diff -- domain_snapshot
git diff -- exercises/06-projects/1.c
```

预期：

```text
make test_csv_store: 通过
make test: 通过
git diff --check: 通过
tests/test_judge.c/tests_test_control.c: no diff
include/record.h/include/judge.h/include/control.h: no diff
src/judge.c/src/control.c: no diff
domain_snapshot: no diff
exercises/06-projects/1.c: no diff
```

## 8. Clean Baseline 要求

Bug Lab 前必须先建立干净基线。

Clean Baseline 必须满足：

```text
1. 主线测试通过。
2. git diff 可解释。
3. 无无关文件改动。
4. 无 domain_snapshot 误改。
5. 无 tests/test_judge.c 误改。
6. 无 tests/test_control.c 误改。
7. 无 record / judge / control 既有文件误改。
8. 无 build/、tmux log 等杂项进入暂存区。
```

建议命令：

```bash
make test_csv_store
make test
git diff --check
git diff -- tests/test_judge.c tests/test_control.c
git diff -- include/record.h include/judge.h include/control.h
git diff -- src/judge.c src/control.c
git diff -- domain_snapshot
git diff -- exercises/06-projects/1.c
git status
```

如果 Clean Baseline 不通过：

```text
禁止进入 Bug Lab。
先修主线。
```

Clean Baseline 通过后，在 docs/daily 中记录：

```yaml
clean_baseline:
  make_test_csv_store: pass
  make_test: pass
  diff_check: pass
  tests_judge_control_diff: none
  record_judge_control_source_diff: none
  domain_snapshot_diff: none
  exercise_reference_diff: none
```

## 9. Bug Lab 注入要求

Bug Lab 是每次闭环结尾的主动训练模块。

Codex 负责设计受控 bug，用户负责亲自注入。

Bug 注入必须满足：

```text
1. 单点注入。
2. 可逆。
3. 可测试触发。
4. 不涉及 domain_snapshot。
5. 不涉及未关闭 gap 的领域规则。
6. 不破坏 Git 历史。
7. 不扩大主线范围。
8. 能训练 C / 文件系统 / Makefile / Git / 408 中至少一个知识点。
9. 用户亲自注入。
10. 用户亲自修复。
```

Codex 必须给出 2-3 个候选 bug：

```text
A. 表头重复写入 bug
B. ftell 判断方向错误
C. fseek 位置错误导致文件大小判断错误
```

每个候选必须包含：

```text
1. 注入位置
2. 用户手动注入方式
3. 预期错误或异常行为
4. 触发命令
5. 恢复方式
6. 对应知识点
7. 风险等级
```

本次推荐 bug：

```yaml
bug_lab_recommendation:
  type: header_duplication_error
  name: csv_header_written_every_append
  injection_point: src/csv_store.c 的 header 写入条件
  injection_action: 用户手动临时去掉 size == 0 判断，使每次 append 都写 header
  trigger_command: make test_csv_store
  expected_error: header 出现 2 次，测试失败
  repair_action: 用户手动恢复为仅当文件为空时写 header
  risk_level: L3
  domain_risk: none
```

训练目标：

```text
1. 理解 CSV 表头属于文件结构元数据。
2. 理解表头应该只在新文件或空文件中写入。
3. 理解 fseek / ftell 如何判断文件是否为空。
4. 理解测试如何捕获重复表头。
5. 理解养殖日志文件需要稳定格式。
```

禁止 Bug Lab 注入：

```text
1. temp_status 阈值类 bug。
2. oxygen_status 领域阈值类 bug。
3. needs_aeration 领域建议类 bug。
4. control_should_aerate 领域决策类 bug。
5. domain_snapshot 类 bug。
6. gap_list 类 bug。
7. 多文件混合 bug。
8. 删除文件类 bug。
9. 硬件控制类 bug。
10. 数据库类 bug。
```

## 10. Bug Lab 修复要求

用户必须亲自修复 bug。

修复流程：

```text
1. 用户手动注入 bug。
2. 用户运行 make test_csv_store。
3. 用户复制报错或异常输出。
4. 用户先自己判断错误类型。
5. Codex 只做提示，不直接给最终答案。
6. 用户手动修复。
7. 用户重新运行 make test_csv_store。
8. 用户重新运行 make test。
9. 用户写 bug_lab。
10. Codex 审查 bug_lab。
```

本次 Bug Lab 文件路径：

```text
bug_lab/2026-05-27-csv-header-duplication.md
```

Bug Lab 模板：

```markdown
# 2026-05-27 CSV Header Duplication Bug Lab

## 1. Bug 注入目标

训练 CSV 表头只写一次的文件结构控制能力。

## 2. 注入位置

src/csv_store.c 的 header 写入条件。

## 3. 注入前 Clean Baseline

记录：
- make test_csv_store:
- make test:
- git diff --check:
- git diff -- tests/test_judge.c tests/test_control.c:
- git diff -- include/record.h include/judge.h include/control.h:
- git diff -- src/judge.c src/control.c:
- git diff -- domain_snapshot:

## 4. 注入动作

临时去掉文件为空时才写 header 的判断，使每次 append 都写入：

```text
pond_id,temp,oxygen
```

## 5. 触发命令

```bash
make test_csv_store
```

## 6. 报错信息或异常行为

粘贴真实报错或测试失败输出。

## 7. 错误类型判断

compile / link / runtime / make / file_io / format_regression / git / domain_policy

本次预期：file_io / format_regression

## 8. 根因分析

解释为什么表头是 CSV 文件结构元数据，不能每条记录前都重复写。

## 9. 修复动作

恢复为：只有当文件为空时才写表头。

## 10. 回归测试

```bash
make test_csv_store
make test
git diff --check
```

## 11. 防复发规则

例如：CSV 表头只属于文件开头；追加数据时只写数据行，不重复写 header。

## 12. SRS 卡片

Q:
A:

## 13. 408 映射

写到王道 408 小节。

## 14. 是否进入长期问题库

是 / 否
```

## 11. docs/daily 要求

docs/daily 必须由用户本人写。

Codex 可以提供模板，但不能直接生成最终成品。

路径：

```text
docs/daily/2026-05-27-csv-header-once-v0.md
```

模板：

```markdown
# 2026-05-27 CSV Header Once v0

## 0. 闭环身份确认

本次 csv_store 表头逻辑修改、测试命令执行、Bug Lab 注入、Bug 修复、docs/daily 与 bug_lab 初稿均由用户本人完成。Codex 仅提供带练、审查和纠偏。

## 1. 今日主线

用户本人手动增强 csv_store，使 CSV 表头只写一次。

## 2. 上一个闭环 SRS 回顾

记录今日开始前抽问的 3-5 张 SRS。

## 3. 领域证据与 gap 边界

- WQ-DO-GROWOUT-001 / SRC-001:
- GAP-001:
- temp_status:
- csv_store 是否新增领域规则:
- csv_store 是否调用 judge/control:
- domain_snapshot 是否修改:

## 4. 用户手动执行步骤

记录实际执行步骤。

## 5. csv_store 模块职责边界

- record:
- csv_store:
- judge:
- control:
- main:
- 今日不做:

## 6. 表头只写一次策略

记录你采用的是：
- fopen 模式:
- fseek:
- ftell:
- header 条件:
- 数据行写入:

## 7. 主线测试结果

```bash
make test_csv_store
make test
git diff --check
```

记录真实结果。

## 8. Clean Baseline

记录以下检查：

```bash
git diff -- tests/test_judge.c tests/test_control.c
git diff -- include/record.h include/judge.h include/control.h
git diff -- src/judge.c src/control.c
git diff -- domain_snapshot
git diff -- exercises/06-projects/1.c
```

## 9. Bug Lab 受控注入

### 9.1 注入目标

### 9.2 注入位置

### 9.3 注入动作

### 9.4 触发命令

### 9.5 报错信息或异常行为

### 9.6 错误类型判断

### 9.7 修复动作

### 9.8 回归测试

## 10. 今日新增 SRS

至少 3-5 张。

## 11. 408 映射

细到王道章节 / 小节 / 知识点。

## 12. Git 提交

记录 git add 白名单和 commit message。

## 13. 明日建议
```

Codex 审查 docs/daily 时只判断：

```text
1. 是否真实记录用户操作。
2. 是否有测试命令和结果。
3. 是否有 Clean Baseline。
4. 是否有 Bug Lab。
5. 是否有 SRS。
6. 是否有 408 映射。
7. 是否没有领域越权。
8. 是否没有把 Codex 代工写成用户闭环。
9. 是否明确 record / csv_store / judge / control / main 的职责边界。
```

## 12. SRS 要求

SRS 分两类：

```text
1. 开始前：上一个闭环抽卡。
2. 结束后：本次闭环新增卡。
```

### 12.1 开始前 SRS

必须从上一轮抽 3-5 张。

格式：

```yaml
- question:
  answer:
  source:
  category:
  review_date:
```

分类：

```text
C 文件操作
FILE*
CSV 持久化
fseek / ftell
Makefile
模块边界
Git
Bug 归因
领域证据
408 数据结构
408 计组
408 操作系统
408 计网
```

### 12.2 结束后 SRS

必须从今日主线和 Bug Lab 抽取。

至少包括：

```text
1. 一个 fseek / ftell 问题。
2. 一个 CSV header 问题。
3. 一个 csv_store 模块职责问题。
4. 一个 bug 归因问题。
5. 一个 408 映射问题。
6. 一个领域证据边界问题。
```

本次建议新增 SRS：

```text
Q1: 为什么 CSV 表头不能每次 append 都写？
A1: 表头是文件结构元数据，只应该出现在文件开头；每次追加都写会污染数据行结构。

Q2: 如何判断一个 CSV 文件是否为空？
A2: 可以打开文件后 fseek 到文件末尾，再用 ftell 获取当前位置；如果位置为 0，说明文件为空。

Q3: fseek(fp, 0, SEEK_END) 的作用是什么？
A3: 将文件位置移动到文件末尾，便于用 ftell 判断文件大小或当前偏移。

Q4: ftell(fp) 返回的是什么？
A4: 返回当前文件位置相对于文件开头的偏移量，可用于判断文件是否为空。

Q5: 为什么表头只写一次仍然不属于领域规则？
A5: 因为它是文件格式规则，不涉及养殖阈值、病害判断、投喂比例或增氧建议。
```

后期可以整理为：

```text
srs/cards/YYYY-MM-DD.yml
```

但当前阶段只要求写进 docs/daily 和 bug_lab。

## 13. 408 映射（详细到王道408的小章节和知识点）

408 映射不能只写大类，必须尽量细到王道小节和具体知识点。

如果无法确定王道精确小节，必须标注：

```text
needs_manual_alignment_with_wangdao
```

不能编造小节号。

本次建议映射：

```yaml
408_mapping:
  data_structure:
    - chapter: "第1章 绪论"
      section: "数据结构的基本概念"
      point: "数据对象、数据元素、数据结构"
      project_mapping: "PondRecord 是内存中的结构化数据对象，csv_store 将其字段序列化为带 header 的 CSV 文本记录。"
      task_evidence: "include/record.h + src/csv_store.c"

    - chapter: "第2章 线性表"
      section: "线性表的定义和基本操作"
      point: "顺序记录与追加"
      project_mapping: "CSV 文件中的 header + 多行数据可视为外存上的顺序记录集合。"
      task_evidence: "tests/test_csv_store.c 检查 header 和多条记录"
      note: "该映射为弱映射，需结合王道版本手动校准。"

  computer_organization:
    - chapter: "第1章 计算机系统概述"
      section: "计算机系统层次结构"
      point: "高级语言程序到可执行程序的转换"
      project_mapping: "Makefile 调用 gcc，把 src/csv_store.c 和 tests/test_csv_store.c 构建成 build/test_csv_store。"
      task_evidence: "Makefile 中 test_csv_store 目标"

    - chapter: "第2章 数据的表示和运算"
      section: "整数与浮点数表示"
      point: "int / float 数据的文本化输出"
      project_mapping: "fprintf 将 PondRecord 中的 pond_id、temp、oxygen 输出为 CSV 文本。"
      task_evidence: "src/csv_store.c 中 fprintf"

    - chapter: "needs_manual_alignment_with_wangdao"
      section: "内存数据与外存表示"
      point: "结构体字段从内存表示转换为文本文件表示"
      project_mapping: "PondRecord 在内存中是结构体，写入 CSV 后变成外存文本记录。"
      task_evidence: "csv_store_append_record"

  operating_system:
    - chapter: "第4章 文件管理"
      section: "文件、目录与路径名"
      point: "文件路径与目录组织"
      project_mapping: "csv_store 通过 filename 定位 CSV 文件，测试使用 build/test_pond_log.csv。"
      task_evidence: "tests/test_csv_store.c"

    - chapter: "第4章 文件管理"
      section: "文件操作"
      point: "文件打开、写入、关闭"
      project_mapping: "fopen / fprintf / fclose 对应 CSV 文件的打开、写入、关闭流程。"
      task_evidence: "src/csv_store.c"

    - chapter: "第4章 文件管理"
      section: "文件定位"
      point: "文件当前读写位置"
      project_mapping: "fseek / ftell 用于判断文件是否为空，从而决定是否写入 header。"
      task_evidence: "src/csv_store.c header once logic"

    - chapter: "第4章 文件管理"
      section: "文件打开方式"
      point: "追加写入与文件结构保持"
      project_mapping: "\"a\" 或 \"a+\" 用于追加记录，header 只在空文件时写入。"
      task_evidence: "bug_lab/2026-05-27-csv-header-duplication.md"

  computer_network:
    - chapter: "不涉及"
      section: "不涉及"
      point: "今日任务无网络通信内容"
      project_mapping: "无"
      task_evidence: "无"

note:
  - "王道版本小节名称可能存在差异，后续需要结合手头教材做 needs_manual_alignment_with_wangdao。"
```

## 14. Git 要求

Git 必须由用户本人执行。

Codex 只能给白名单。

提交前必须执行：

```bash
git status
git diff
git diff --check
make test_csv_store
make test
```

如果有暂存：

```bash
git diff --cached
```

允许 add 的文件：

```bash
git add codex/daily_packets/005-csv-header-once-v0.md
git add src/csv_store.c
git add tests/test_csv_store.c
git add docs/daily/2026-05-27-csv-header-once-v0.md
git add bug_lab/2026-05-27-csv-header-duplication.md
```

如果 Makefile 没有改动，不要 add Makefile。

禁止 add：

```text
build/
tmux-*.log
domain_snapshot/
exercises/06-projects/1.c
tests/test_judge.c
tests/test_control.c
include/record.h
include/judge.h
include/control.h
include/csv_store.h
src/judge.c
src/control.c
input_cli / output / main / hardware / database / frontend 相关文件
```

推荐 commit message：

```bash
git commit -m "feat: add csv header once loop"
```

或分两个 commit：

```bash
git commit -m "feat: write csv header once"
git commit -m "docs: record csv header duplication bug lab"
```

若保持每日闭环单 commit，使用：

```bash
git commit -m "feat: complete guided csv header loop"
```

## 15. Codex 第一轮输出要求

Codex 第一轮只允许输出计划，不允许修改文件。

必须按以下格式：

```markdown
# Codex 第一轮：005-csv-header-once-v0 执行计划

## 1. 我已读取的文件

列出已读取文件。

## 2. 上一个闭环 SRS 抽卡

### Q1

### Q2

### Q3

### Q4

### Q5

等待用户回答，或允许用户选择跳过到主线。

## 3. 当前任务状态判断

判断 csv_store 是否已完成 append。
判断 test_csv_store 是否已有两条记录测试。
判断当前是否已有 header 逻辑。
判断领域边界。

## 4. 今日主线最小目标

说明今日只做 CSV 表头只写一次。

## 5. 用户需要亲自执行的步骤

给出用户需要手动执行的 nvim / make / git diff 命令。

## 6. Codex 只负责带练的内容

说明 Codex 不改文件，只解释和审查。

## 7. 明确禁止 Codex 代工的内容

明确不允许 Codex 修改 csv_store、测试、docs、bug_lab、domain_snapshot。

## 8. Clean Baseline 要求

列出用户主线完成后要执行的检查。

## 9. 今日 Bug Lab 候选

A. 表头重复写入 bug
B. ftell 判断方向错误
C. fseek 位置错误导致文件大小判断错误

推荐：A

## 10. 等待用户确认

请用户回复：
A. 开始主线手动闭环
B. 只做 SRS 抽卡
C. 只做 Bug Lab 设计
```

用户确认 A 后，Codex 仍然不改文件，只给用户手动操作指导。

## 16. Codex 只能审查的内容

Codex 只能审查：

```text
1. 用户贴出的 git diff。
2. 用户贴出的 make/gcc 输出。
3. 用户写的 docs/daily 草稿。
4. 用户写的 bug_lab 草稿。
5. 用户写的 SRS 卡片。
6. 用户给出的 408 映射。
7. 用户准备 git add 的文件列表。
```

Codex 审查时必须输出：

```text
1. 是否合格。
2. 不合格点。
3. 是否违反领域证据规则。
4. 是否违反最小闭环规则。
5. 是否存在 Codex 代工痕迹。
6. Bug Lab 是否完成受控注入、触发、修复、回归。
7. SRS 是否来自真实问题。
8. 408 映射是否细到王道小节。
9. csv_store 是否只做 CSV header 和 record 持久化，没有调用 judge/control。
10. 下一步用户亲自做什么。
```

Codex 不允许：

```text
1. 直接修。
2. 直接写。
3. 直接提交。
4. 替用户完成闭环。
5. 替用户注入 bug。
6. 替用户修 bug。
7. 替用户修改 csv_store。
```

最终完成标准：

```text
用户本人完成 CSV header once 逻辑。
用户本人完成 test_csv_store 增强。
用户本人完成主线测试。
用户本人完成 Clean Baseline。
用户本人完成 Bug Lab 注入与修复。
用户本人写 docs/daily。
用户本人写 bug_lab。
Codex 只完成审查。
Git 提交只包含白名单文件。
```
